enum Sex {
    Male
    Female
}

enum Role {
    User
    Admin
}

enum AuthStatus {
    FAILED
    SUCCESS
    ERROR
}

enum OTPType {
    AccountVerification
    OrderVerification
    ResetPassword
}

enum PreferredMeansOfNotification {
    Email
    SMS
    # Whatsapp
}

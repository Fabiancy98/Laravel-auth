enum Sex {
    Male
    Female
}

enum Role {
    User
    Admin
    SuperAdmin
}

enum AuthStatus {
    FAILED
    SUCCESS
    ERROR
}

enum OTPType {
    AccountVerification
    ResetPassword
}

enum PreferredMeansOfNotification {
    Email
    SMS
    # Whatsapp
}
